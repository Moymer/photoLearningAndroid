package moymer.com.utils

import moymer.com.photolearning.BuildConfig

class Constants {

    companion object {
        const val sServiceAddress = BuildConfig.serviceAddress

        const val sIntervalPublicAddress = BuildConfig.intervalPublicAddress
        const val sInterval = BuildConfig.interval

        const val sCloudFrontAddress = BuildConfig.cloudFrontAddress


    }
}